export enum Tables {
  users = 'users',
  departments = 'departments',
  tasks = 'tasks',
}

export enum Columns {
  id = 'id',
  createdAt = 'createdAt',
  updatedAt = 'updatedAt',

  name = 'name',
  email = 'email',
  password = 'password',

  title = 'title',
  description = 'description',

  authorId = 'authorId',
}
